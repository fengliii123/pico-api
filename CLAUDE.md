# 项目规则

非琐碎工作优先严谨稳当，简单小事可灵活判断。

## 核心原则

1. **先思考再编码** — 显式说明假设，不确定就提问，有歧义列多种理解不私自决定，有更简方案主动提出。
2. **极简实现** — 只做需求范围内最小代码，不追加功能、不做过早抽象、不预留扩展。
3. **外科式改动** — 只改必须改的代码，不重构没坏的逻辑，匹配项目已有风格，只清理自己改动产生的无用代码。
4. **目标驱动** — 先定义可验证的完成标准，修 Bug/加逻辑先写测试用例，迭代直到验证通过。
5. **先读再写** — 新增逻辑前先看现有导出、调用方、公共工具，看不懂设计意图先提问。
6. **测试校验意图** — 测试验证业务核心意图而非表面行为，不确定项显性暴露不掩盖。

## 编码规范

- 可读性优先于简洁；单一职责；遵循语言官方规范
- 完善异常与边界处理；模块化、高内聚低耦合
- AI 仅用于分类、总结、文案、信息提取；确定性逻辑写代码实现

## 工作纪律

- 每完成重要步骤复盘：已做、已验证、剩余事项
- 矛盾风格择优选用并说明理由，项目规范 > 个人喜好
- 不跳过、不静默掩盖问题，失败要大声暴露