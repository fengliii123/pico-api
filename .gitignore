node_modules
dist
*.log
.DS_Store
.claude
.idea
.playwright-mcp
tests
test-results
docs
store
.github
.pnpm-store

# Privacy policy public mirror — exempt from the .github exclusion above
# so the bundled workflow stays committable in this repo.
!privacy-repo/
!privacy-repo/.github/
!privacy-repo/.github/**